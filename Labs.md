<h1> Лабораторна работа 1 <h1>
  
<br>Задание:  
- Загрузить данные из файла ncr_uber_rides_bookings.csv  
-  Вывести первые 5 строк для ознакомления  
-  Получить общую информацию о датасете  
-  Вывести статистическое описание числовых столбцов  
-  Определить количество строк и столбцов  
<br>

````
import pandas as pd
import numpy as np

class DataAnalyzer:
    def __init__(self, file_path):
        self.file_path = file_path
        self.df = None
    
    def load_data(self):
        """Загрузка данных из CSV файла"""
        try:
            self.df = pd.read_csv(self.file_path)
            print("✅ Данные успешно загружены")
        except Exception as e:
            print(f"❌ Ошибка загрузки: {e}")
    
    def basic_analysis(self):
        """Проведение базового анализа данных"""
        if self.df is None:
            print("❌ Данные не загружены")
            return
        
        print("\n" + "="*50)
        print("1. ПЕРВЫЕ 5 СТРОК ДАННЫХ:")
        print("="*50)
        print(self.df.head())
        
        print("\n" + "="*50)
        print("2. ОБЩАЯ ИНФОРМАЦИЯ О ДАННЫХ:")
        print("="*50)
        print(self.df.info())
        
        print("\n" + "="*50)
        print("3. СТАТИСТИЧЕСКОЕ ОПИСАНИЕ:")
        print("="*50)
        print(self.df.describe())
        
        print("\n" + "="*50)
        print("4. СТРУКТУРА ДАННЫХ:")
        print("="*50)
        print(f"Количество строк: {self.df.shape[0]}")
        print(f"Количество столбцов: {self.df.shape[1]}")
        
        print("\n" + "="*50)
        print("5. АНАЛИЗ ПРОПУЩЕННЫХ ЗНАЧЕНИЙ:")
        print("="*50)
        missing_data = self.df.isnull().sum()
        for col, missing_count in missing_data.items():
            print(f"{col}: {missing_count} пропущенных значений")
        
        print("\n" + "="*50)
        print("6. УНИКАЛЬНЫЕ ЗНАЧЕНИЯ КАТЕГОРИАЛЬНЫХ ПЕРЕМЕННЫХ:")
        print("="*50)
        categorical_columns = ['Booking Status', 'Vehicle Type']
        for col in categorical_columns:
            if col in self.df.columns:
                unique_values = self.df[col].unique()
                print(f"\n{col}: {len(unique_values)} уникальных значений")
                print(f"Значения: {list(unique_values)}")

# Запуск анализа
if __name__ == "__main__":
    analyzer = DataAnalyzer('ncr_uber_rides_bookings.csv')
    analyzer.load_data()
    analyzer.basic_analysis()
````
<br>Результаты выполнения

-  Данные успешно загружены из CSV файла  
-  Проведен первичный анализ структуры данных  
-  Выявлены пропущенные значения в каждом столбце  
-  Проанализированы уникальные значения категориальных переменных  
<br> 
<br>
<br>
<br>

<h1>Лабораторная работа 2: Выборка и фильтрация данных<h1>
<br> Задание:  
Разработать современный графический интерфейс для приложения прогнозирования стоимости поездок Uber  
<br>
ОПИСАНИЕ РЕАЛИЗАЦИИ:  
<br>
Архитектура интерфейса:    
-  Главное окно с вкладками для разных функций  
-  Левая панель - калькулятор поездок  
-  Правая панель - область результатов и графиков  
-  Современная цветовая схема (#2c3e50, #3498db, #e74c3c)  
<br>
Ключевые элементы дизайна:  
<br>

  ````
# Стилизация виджетов
style.configure('Modern.TFrame', background='#2c3e50')
style.configure('Modern.TLabel', background='#2c3e50', foreground='#ecf0f1')
style.configure('Accent.TButton', background='#e74c3c', foreground='white')

# Структура интерфейса
- Header с заголовком и описанием
- Control panel с кнопками управления
- Calculator panel для ввода параметров
- Results area с вкладками для логов и графиков
- Status bar для отображения состояния системы
````
<br>
Функциональные блоки:  
1. Панель управления - кнопки загрузки данных, обучения модели, расчета  
2. Калькулятор - форма ввода параметров поездки с валидацией  
3. Визуализация - область для графиков и статистики  
4. Логирование - системные сообщения и результаты работы  

<h1> ЛАБОРАТОРНАЯ РАБОТА №3 <h1>  
<br>Задание:  
Написать скрипт для вывода статистического обзора данных, подсчета пропущенных значений и анализа категориальных переменных  
<br>
  
````
import pandas as pd
import numpy as np

def uber_statistical_analysis(file_path):
    """
    Комплексный статистический анализ данных Uber
    """
    # Загрузка данных
    df = pd.read_csv(file_path)
    
    print("=" * 70)
    print("СТАТИСТИЧЕСКИЙ АНАЛИЗ ДАННЫХ UBER")
    print("=" * 70)
    
    # 1. Базовая информация
    print("\n📊 1. БАЗОВАЯ ИНФОРМАЦИЯ О ДАННЫХ")
    print("-" * 40)
    print(f"Общее количество записей: {df.shape[0]:,}")
    print(f"Количество признаков: {df.shape[1]}")
    print(f"Размер данных: {df.memory_usage(deep=True).sum() / 1024**2:.2f} MB")
    
    # 2. Статистика числовых колонок
    print("\n📈 2. СТАТИСТИКА ЧИСЛОВЫХ ПОКАЗАТЕЛЕЙ")
    print("-" * 40)
    numeric_cols = df.select_dtypes(include=[np.number]).columns
    print("Доступные числовые колонки:", list(numeric_cols))
    
    if 'Booking Value' in df.columns:
        booking_stats = df['Booking Value'].describe()
        print(f"\n💰 Статистика стоимости поездок:")
        print(f"   • Средняя стоимость: {booking_stats['mean']:.2f} руб.")
        print(f"   • Медиана: {booking_stats['50%']:.2f} руб.") 
        print(f"   • Стандартное отклонение: {booking_stats['std']:.2f} руб.")
        print(f"   • Диапазон: {booking_stats['min']:.2f} - {booking_stats['max']:.2f} руб.")
    
    # 3. Анализ пропущенных значений
    print("\n🔍 3. АНАЛИЗ ПРОПУЩЕННЫХ ЗНАЧЕНИЙ")
    print("-" * 40)
    
    missing_data = df.isnull().sum()
    total_missing = missing_data.sum()
    
    if total_missing > 0:
        print("Колонки с пропущенными значениями:")
        for col, count in missing_data.items():
            if count > 0:
                percentage = (count / len(df)) * 100
                print(f"   • {col}: {count} пропусков ({percentage:.2f}%)")
    else:
        print("✅ Пропущенных значений не обнаружено")
    
    # 4. Анализ категориальных переменных
    print("\n🎯 4. АНАЛИЗ КАТЕГОРИАЛЬНЫХ ПЕРЕМЕННЫХ")
    print("-" * 40)
    
    # Booking Status анализ
    if 'Booking Status' in df.columns:
        print("📋 СТАТУСЫ БРОНИРОВАНИЙ:")
        status_counts = df['Booking Status'].value_counts()
        print(f"   Уникальных статусов: {len(status_counts)}")
        
        for status, count in status_counts.items():
            percentage = (count / len(df)) * 100
            print(f"   • {status}: {count} ({percentage:.1f}%)")
    else:
        print("❌ Колонка 'Booking Status' не найдена")
    
    # Vehicle Type анализ  
    if 'Vehicle Type' in df.columns:
        print("\n🚗 ТИПЫ ТРАНСПОРТА:")
        vehicle_counts = df['Vehicle Type'].value_counts()
        print(f"   Уникальных типов: {len(vehicle_counts)}")
        
        for vehicle, count in vehicle_counts.items():
            percentage = (count / len(df)) * 100
            print(f"   • {vehicle}: {count} ({percentage:.1f}%)")
    else:
        print("❌ Колонка 'Vehicle Type' не найдена")
    
    # 5. Общая сводка
    print("\n📋 5. ОБЩАЯ СВОДКА")
    print("-" * 40)
    print(f"Всего записей: {len(df):,}")
    print(f"Пропущенных значений: {total_missing}")
    print(f"Заполненность данных: {((1 - total_missing/(len(df)*len(df.columns)))*100):.1f}%")

````
<br>
РЕЗУЛЬТАТЫ ВЫПОЛНЕНИЯ:  
<br>
Скрипт успешно выполняет:  
-  Статистический обзор числовых показателей  
-  Подсчет пропущенных значений по каждому столбцу  
- Анализ уникальных значений категориальных переменных  
-  Формирование комплексной сводки по данным  
<br>
Выходные данные включают:  
-  Базовую информацию о наборе данных  
-  Описательную статистику числовых колонок  
-  Детальный анализ пропусков с процентами  
-  Распределение по статусам бронирования и типам транспорта  
-  Общую оценку качества данных  
